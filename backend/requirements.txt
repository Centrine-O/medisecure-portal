fastapi==0.104.1  - main web framework for buidling API
uvicorn[standard]==0.24.0   - Web server that runs FastAPI
sqlalchemy==2.0.23              - Talks to PostgreSQL database
psycopg2-binary==2.9.9       - PostgreSQL driver (connects Python to Postgres)
alembic==1.12.1         - Database migrations (tracks schema changes)
pydantic==2.5.0          - Data validation (makes sure form data is correct)
pydantic-settings==2.1.0 
python-jose[cryptography]==3.3.0   - Creates JWT tokens for authentication
passlib[bcrypt]==1.7.4 - Hashes passwords securely (bcrypt)
python-multipart==0.0.6
redis==5.0.1            -Connects to Redis for caching
celery==5.3.4  - Background task queue (sends emails, processes data)
cryptography==41.0.7 - Encrypts sensitive data (SSN, cards)
python-dotenv==1.0.0 
pytest==7.4.3  -Testing framework
httpx==0.25.2 - Makes HTTP requests (for testing APIs)